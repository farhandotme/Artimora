<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Page</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      body {
        background-image: url("../images/van-gogh-almond-blossom-pzkdt1ub1mjh2yhf.jpg");
        background-size: cover;
        background-position: center;
      }
    </style>
  </head>
  <body class="flex flex-col items-center justify-center h-screen space-y-4">
    <!-- Flash Messages -->
    <% if (messages.error) { %>
      <div
        class="flash-message bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded-lg shadow-md w-full max-w-md"
        role="alert"
      >
        <span class="block sm:inline"><%= messages.error %></span>
      </div>
    <% } %>
    <% if (messages.success) { %>
      <div
        class="flash-message bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded-lg shadow-md w-full max-w-md"
        role="alert"
      >
        <span class="block sm:inline"><%= messages.success %></span>
      </div>
    <% } %>

    <!-- Login Form -->
    <div class="bg-white/70 rounded-xl shadow-lg p-10 w-full max-w-md">
      <h1 class="text-center text-2xl font-semibold text-blue-700 mb-2">
        Login Page
      </h1>
      <h2 class="text-center text-3xl font-bold text-gray-800 mb-8">
        ARTIMORA
      </h2>
      <form action="/login" method="POST">
        <div class="mb-4">
          <input
            type="email"
            name="email"
            placeholder="Your Email"
            class="w-full p-3 rounded-full border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>
        <div class="mb-4">
          <input
            type="password"
            name="password"
            placeholder="Enter Password"
            class="w-full p-3 rounded-full border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500"
          />
        </div>
        <button
          type="submit"
          class="w-full bg-blue-500 text-white font-semibold py-3 rounded-full hover:bg-blue-600 transition duration-300"
        >
          Login
        </button>
        <p class="text-center mt-4">Dont have an account? <a class="text-blue-500" href="/register">Register</a></p>
      </form>
    </div>
  </body>
</html>
